export class PlayerLike {
    postId: string;
    playerEmail: string;

    constructor(postId: string, playerEmail: string) {
        this.postId = postId;
        this.playerEmail = playerEmail;
    }
}
