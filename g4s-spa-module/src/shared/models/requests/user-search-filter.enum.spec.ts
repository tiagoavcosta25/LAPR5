import { UserSearchFilter } from './user-search-filter.enum';

describe('UserSearchFilter', () => {
  it('should create an instance', () => {
    expect(new UserSearchFilter()).toBeTruthy();
  });
});
