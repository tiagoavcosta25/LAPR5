import { CreatingDirectRequest } from './creating-direct-request.model';

describe('CreatingDirectRequest', () => {
  it('should create an instance', () => {
    expect(new CreatingDirectRequest()).toBeTruthy();
  });
});
