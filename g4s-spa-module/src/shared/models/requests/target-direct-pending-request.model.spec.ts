import { TargetDirectPendingRequest } from './target-direct-pending-request.model';

describe('TargetDirectPendingRequest', () => {
  it('should create an instance', () => {
    expect(new TargetDirectPendingRequest()).toBeTruthy();
  });
});
