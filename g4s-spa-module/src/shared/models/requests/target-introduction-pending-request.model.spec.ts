import { TargetIntroductionPendingRequest } from './target-introduction-pending-request.model';

describe('TargetIntroductionPendingRequest', () => {
  it('should create an instance', () => {
    expect(new TargetIntroductionPendingRequest()).toBeTruthy();
  });
});
