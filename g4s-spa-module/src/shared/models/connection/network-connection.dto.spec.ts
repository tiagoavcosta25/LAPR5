import { NetworkConnection } from './network-connection.dto';

describe('NetworkConnection', () => {
  it('should create an instance', () => {
    expect(new NetworkConnection()).toBeTruthy();
  });
});
