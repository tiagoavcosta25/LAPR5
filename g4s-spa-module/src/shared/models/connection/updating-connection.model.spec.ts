import { UpdatingConnection } from './updating-connection.model';

describe('UpdatingConnection', () => {
  it('should create an instance', () => {
    expect(new UpdatingConnection()).toBeTruthy();
  });
});
