import { GettingConnection } from './getting-connection.model';

describe('GettingConnection', () => {
  it('should create an instance', () => {
    expect(new GettingConnection()).toBeTruthy();
  });
});
