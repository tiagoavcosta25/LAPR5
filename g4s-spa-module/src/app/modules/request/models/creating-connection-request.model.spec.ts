import { CreatingConnectionRequest } from './creating-connection-request.model';

describe('CreatingConnectionRequest', () => {
  it('should create an instance', () => {
    expect(new CreatingConnectionRequest()).toBeTruthy();
  });
});
